%% -*- mode: Erlang; fill-column: 80; comment-column: 75; -*-
{erl_opts, [
            debug_info,
            {parse_transform, lager_transform}
           ]}.

{deps, [
        {lager, "3.5.2"}
       ]}.

{shell, [
         {apps, [
                 lager,
                 db
                ]}
         ]}.
